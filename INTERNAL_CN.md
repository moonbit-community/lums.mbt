# Internal

## Patience Diff的实现与工作原理

Patience Diff这个算法最早由Bram Cohen提出, 它基本上是一个可以和其他diff算法协作的启发式文本分割策略，其核心想法如下:

- 对于文本块old和new, 首先计算每一行的出现次数，然后在old和new内部分别找只出现过一次的特殊行作为候选锚点

假如old是这样

```
1| Ruby: ruby-lang.org
2| #
3| Python: python.org
4| #
5| MoonBit: www.moonbitlang.com
6| #
7| Perl: use.perl.org
```

在这个例子中, `#`在old的全文出现了不止一次, 所以它算不上独一无二。其余四行在old中只出现过一次，可以作为候选锚点。

- 然后，针对两个文本块中找到的候选锚点，选出在两边都存在的锚点作为候选锚点序列

假如new是这样:

```
1| Python: python.org
2| #
3| MoonBit: www.moonbitlang.com
4| #
5| Javascript: tc39.es
6| #
7| Ruby: ruby-lang.org
```

那么候选锚点序列就是:

```
"Ruby: ruby-lang.org": old index = 1, new index = 7
"Python: python.org": old index = 3, new index = 1
"MoonBit: www.moonbitlang.com": old index = 5, new index = 3
```

候选锚点序列需要保证有一列`index`是有序的, 此处我们约定按照`old index`的升序从上往下排列。

- 接着, 在这个候选序列中，按照`new index`搜索一个最长的升序序列，这样一来两边的index都是升序了。

从上面的候选锚点序列搜索出来的序列是:

```
"Python: python.org": old index = 3, new index = 1
"MoonBit: www.moonbitlang.com": old index = 5, new index = 3
```

- 最后，按照锚点把两块文本切开，对切出来的子区间使用一个基础的diff算法进行处理

网上还有另外一种做法: 对子区间再次应用patience，直到再也找不到合适的锚点。这种做法是Bram Cohen提出的第一版Patience, 他在后来的博客中认为一次分割在实际使用效果上并没有显得比递归分割显著更差，但是更简单一些，所以此处仍采用一次分割法。

它的基本原理就是这么简单! 并且在许多真实的代码编辑中，这个启发式的策略效果良好。

其实现方式基本上就是上面的这几步流程，唯一较为复杂的地方可能是搜索最长的单调序列，这个地方用到了一个叫`Patience sort`的算法(也是Patience diff名字的来源). 

### Patience排序

`Patience Sort`的名字据说来自于一个叫`Patience`的卡牌接龙游戏，游戏开始时有一副洗好的牌(对应无序的new index列表)，按照几条规则将牌一张接一张地发到桌面上的一系列牌堆中，然后最长递增子序列就找好了。

下面使用一个包含1到13的数组来作为例子(每个数都只出现一次，因为它们都是筛选出的候选锚点)

```
5   9   4   6   12   8   7   1   10   11   3   2   13
```

第一步，将5取出，由于现在桌面上空空如也，新建一个牌堆(pile)来放它。

```
9   4   6   12   8   7   1   10   11   3   2   13
-------------------------------------------------


5
```

下一步，取出9，因为9比5大，所以它不能放到5的顶端，只能放到5的右边，再起一个新牌堆。这一次与第一次的不同之处在于，我们要记录一些额外的信息。`9`最后一个做比较的数是`5`, 所以要新建一个从`9`指向`5`的*back pointer*.



```
4   6   12   8   7   1   10   11   3   2   13       9 -> 5
---------------------------------------------


5   9
```

再下一步，取出4, 4小于5，那么直接把4放到5的上面，不用记录back pointer

```
6   12   8   7   1   10   11   3   2   13      9 -> 5
-----------------------------------------


4
5   9
```

接下来的操作可以如法炮制，取出6，6大于4但是小于9，所以放到9的顶部并记录一个从6到4的back pointer.

```
12   8   7   1   10   11   3   2   13      9 -> 5
-------------------------------------      6 -> 4


4   6
5   9
```

不断重复这个过程，最终得到这样的牌堆和back pointer记录

```
  9   ->  5
  6   ->  4
  12  ->  6
  8   ->  6
  7   ->  6
  10  ->  7
  11  ->  10
  3   ->  1
  2   ->  1
  13  ->  11

    2                 
1   3    7
4   6    8
5   9   12   10   11   13
```

最后，从最右边顶部的13沿着back pointer的指向开始搜索: 13 -> 11 -> 10 -> 7 -> 6 -> 4, 将这个序列翻转过来就得到了最长的单调递增序列。

在将这个过程转化成代码的时候，有一个优化可以做，因为这些牌堆顶部的元素是有序的，所以在牌堆的数量较多时可以使用二分法查找新牌的最终存放位置